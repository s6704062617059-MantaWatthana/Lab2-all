#include <stdio.h>

int main() {
    int a, b, c, d;

    // 4.1
    a=1; b=1; c=5; d=1;
    printf("Set 1:\n");
    printf("A = %f\n", (float)(a+b)/c);
    printf("B = %d\n", a*b+c-d);
    return 0;
}
